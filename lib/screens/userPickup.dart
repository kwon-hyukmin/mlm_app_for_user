import 'package:flutter/material.dart';

void main(){
  runApp(const UserPickUp());
}

class UserPickUp extends StatelessWidget {
  const UserPickUp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
